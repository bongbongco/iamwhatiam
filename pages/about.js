import Head from "next/head";
import withLayout from "../lib/withLayout";

export default () => (
    <div>
        <Head>
            <title>About | IamwhatIam Store</title>
        </Head>
        <h1>About Page</h1>
    </div>
);